{
  "name": "seenrelay",
  "version": "0.3.10",
  "description": "Avoid redundant expensive validation. CHECK recent evidence; OBSERVE fresh independent results.",
  "mcpServers": {
    "seenrelay": {
      "httpUrl": "https://seenrelay.com/mcp",
      "description": "Avoid redundant expensive validation with recent source-backed evidence.",
      "includeTools": [
        "check_fact",
        "observe_fact"
      ]
    }
  }
}
