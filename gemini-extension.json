{
  "name": "seenrelay",
  "version": "0.3.10",
  "description": "Find repeated expensive read-only checks in AI agent workloads before policy-gated reuse.",
  "mcpServers": {
    "seenrelay": {
      "httpUrl": "https://seenrelay.com/mcp",
      "description": "Find repeated expensive read-only checks in AI agent workloads before policy-gated reuse.",
      "includeTools": [
        "check_fact",
        "observe_fact"
      ]
    }
  }
}
