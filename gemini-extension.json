{
  "name": "seenrelay",
  "version": "0.3.10",
  "description": "Measure repeated expensive read-only validation across AI agent fleets before policy-gated reuse.",
  "mcpServers": {
    "seenrelay": {
      "httpUrl": "https://seenrelay.com/mcp",
      "description": "Measure repeated expensive read-only validation across AI agent fleets before policy-gated reuse.",
      "includeTools": [
        "check_fact",
        "observe_fact"
      ]
    }
  }
}
