# Infrastructure cost guardrail smoke test

This branch-only file exists solely to verify that documentation-only changes do not trigger Vercel builds. It is not intended for merge.

Second documentation-only commit: Vercel now has a previous SHA for this branch, so the ignored-build rule can be exercised rather than failing open on first deploy.
