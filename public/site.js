(()=>{'use strict';const nf=new Intl.NumberFormat();const pct=n=>`${(Number(n||0)*100).toFixed(2)}%`;function set(sel,value){document.querySelectorAll(sel).forEach(el=>{el.textContent=value})}async function load(){try{const r=await fetch('/public-stats.json',{headers:{accept:'application/json'},cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);const s=await r.json();for(const [k,v] of Object.entries(s)){set(`[data-stat="${k}"]`,nf.format(Number(v||0)));set(`[data-stat-pct="${k}"]`,pct(v))}const t=document.getElementById('stats-time');if(t)t.textContent=new Date(s.generated_at).toLocaleString()}catch{const t=document.getElementById('stats-time');if(t)t.textContent='temporarily unavailable'}}load();setInterval(load,30000)})();
